pipeline {
    agent any
    stages {
        stage('Terraform Init') { steps { echo 'Initializing Terraform...' } }
        stage('Docker Build') { steps { echo 'Building Docker Images...' } }
    }
}
